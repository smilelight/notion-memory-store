# Notion Memory Store

Persistent cross-platform memory layer using Notion as backend. Store, recall, and manage
AI conversation memories across Claude.ai, Claude Code, and OpenClaw.

## Features

- **Write memories**: Decompose conversations into atomic, searchable memory entries
- **Recall memories**: Smart topic-driven recall with relevance ranking
- **Cross-platform**: Works with Claude.ai, Claude Code, and OpenClaw
- **Deduplication**: Automatically detects duplicates and conflicting memories
- **Expiry management**: Memories can have TTLs (30d, 90d, 1y, Never)

## Prerequisites

- [Claude Code](https://claude.ai/claude-code) installed
- A Notion workspace with API access
- Notion MCP server connected (via `.mcp.json` or Claude Code settings)

## Installation

### As a Claude Code Plugin

```bash
claude plugin add /path/to/notion-memory-store
```

Or clone and add:

```bash
git clone https://github.com/smilelight/notion-memory-store.git
claude plugin add ./notion-memory-store
```

## Setup

Run the setup command after installation:

```
/setup-memory-store
```

This will:
1. Check your Notion MCP connection
2. Search for or create a "Memory Store" database in your Notion workspace
3. Write the config to `~/.config/memory-store/config.json`

## Usage

### Write Memories

Say any of these to trigger memory archival:
- "总结记忆" / "summarize memory"
- "归档对话" / "同步记忆到Notion"
- Or invoke: `/memory-to-notion`

### Recall Memories

The recall skill triggers **proactively** when context would help. You can also say:
- "回忆一下" / "recall"
- "你还记得吗" / "之前我们讨论过"
- Or invoke: `/recall-from-notion`

## Configuration

Config file: `~/.config/memory-store/config.json`

```json
{
  "notion": {
    "data_source_id": "<your-database-data-source-id>",
    "database_url": "<your-database-url>"
  }
}
```

This file is auto-generated by `/setup-memory-store`. You can also create it manually
if you already have a Memory Store database.

## Database Schema

| Property    | Type         | Description                            |
|-------------|-------------|----------------------------------------|
| Title       | Title       | One-line memory summary                |
| Category    | Select      | Fact / Decision / Preference / Context / Pattern / Skill |
| Tags        | Multi-select| Extensible tag set                     |
| Content     | Rich Text   | Detailed memory content                |
| Source      | Select      | Claude.ai / ClaudeCode / Manual / OpenClaw / Other |
| Confidence  | Select      | High / Medium / Low                    |
| Status      | Select      | Active / Archived / Contradicted       |
| Scope       | Select      | Global / Project                       |
| Project     | Rich Text   | Project name (when Scope = Project)    |
| Expiry      | Select      | Never / 30d / 90d / 1y                |
| Source Date | Date        | When the original conversation happened|
| Last Used   | Date        | Last recall timestamp                  |
| Memory ID   | Rich Text   | MEM-0001 format identifier             |

## Plugin Structure

```
notion-memory-store/
├── .claude-plugin/
│   └── plugin.json                # Plugin metadata
├── skills/
│   ├── memory-to-notion/
│   │   └── SKILL.md               # Write memories to Notion
│   └── recall-from-notion/
│       └── SKILL.md               # Recall memories from Notion
├── commands/
│   └── setup-memory-store.md      # /setup-memory-store command
├── README.md
└── LICENSE
```

## License

MIT
